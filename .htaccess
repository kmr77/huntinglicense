
# BEGIN WordPress
# "BEGIN WordPress" から "END WordPress" までのディレクティブ (行) は
# 動的に生成され、WordPress フィルターによってのみ修正が可能です。
# これらのマーカー間にあるディレクティブへのいかなる変更も上書きされてしまいます。
<IfModule mod_rewrite.c>
RewriteEngine On

# 🔁 リダイレクトルール（WordPressより前に記述）
RewriteRule ^ami/?$ /category/ami/ [R=301,L]
RewriteRule ^all/?$ /category/all/ [R=301,L]
RewriteRule ^examination/?$ /category/examination/ [R=301,L]
RewriteRule ^laws/?$ /category/laws/ [R=301,L]
RewriteRule ^numbers/?$ /category/numbers/ [R=301,L]
RewriteRule ^wana/?$ /category/wana/ [R=301,L]

# 🔧 WordPress 標準ルール（そのまま）
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
RewriteBase /huntinglicense/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /huntinglicense/index.php [L]
</IfModule>


# END WordPress